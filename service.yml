apiVersion: v1
kind: Service
metadata:
  name: flask-hello-service
  namespace: flask-app
spec:
  selector:
    app: flask-hello
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
